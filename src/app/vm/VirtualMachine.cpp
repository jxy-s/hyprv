// VirtualMachine is now a plain-old-data struct; all population goes
// through VMManager::UpdateAll using Msvm_VirtualSystemManagementService::
// GetSummaryInformation. No translation unit content needed at the moment.

#include "VirtualMachine.h"
